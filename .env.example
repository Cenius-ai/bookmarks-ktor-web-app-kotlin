# Bookmarker configuration
# All values have sensible defaults — no .env file required to run.

# Server port (default: 8080)
PORT=8080

# Database URL (default: jdbc:sqlite:bookmarks.db)
# DATABASE_URL=jdbc:sqlite:bookmarks.db
